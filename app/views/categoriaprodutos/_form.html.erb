
<%= form_for(@categoriaproduto) do |f| %>
  <% if @categoriaproduto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoriaproduto.errors.count, "error") %> prohibited this categoriaproduto from being saved:</h2>

      <ul>
      <% @categoriaproduto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Cadastar Categoria</h3>
  </div>
  <div class="panel-body">

 <div class="row">
  <div class="col-lg-5">
    <%= f.label :nome %><br>
    <%= f.text_field :nome, :class => 'form-control' %>
  </div>
 </div>

  </div>
</div>

<div class="btn-group">
  <button type="submit" value="Submit" class="btn btn-primary btn-sm">
  <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
  </button>
  <%= link_to categoriaprodutos_path, :class => "btn btn-default btn-sm" do %>
      <span class="glyphicon glyphicon-arrow-left"></span> Voltar
    <% end %>
</div>

<% end %>

